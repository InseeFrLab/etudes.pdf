% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/couleurs.R
\name{hex_alpha_to_rgb}
\alias{hex_alpha_to_rgb}
\title{Convertit un code hexadécimal avec transparence (8 caractères) en code hexadécimal standard (6 caractères)}
\usage{
hex_alpha_to_rgb(hex_alpha, bg_hex = "#FFFFFF")
}
\arguments{
\item{hex_alpha}{Chaîne de caractères au format \verb{#RRGGBBAA} (ex: \code{"#80FF00CC"}).}

\item{bg_hex}{Chaîne de caractères au format \verb{#RRGGBB} représentant la couleur d'arrière-plan
(par défaut \code{"#FFFFFF"} pour blanc).}
}
\value{
Une chaîne de caractères au format \verb{#RRGGBB}.
}
\description{
Cette fonction prend un code couleur hexadécimal au format \verb{#RRGGBBAA} (avec canal alpha)
et le convertit en un code \verb{#RRGGBB} équivalent, en simulant la fusion avec un arrière-plan
spécifié (par défaut blanc). La conversion suit le modèle de mélange alpha standard.
}
\note{
\itemize{
\item Si \code{hex_alpha} est déjà opaque (\code{AA = "FF"}), la couleur retournée est identique à \verb{#RRGGBB}.
\item Si \code{hex_alpha} est totalement transparent (\code{AA = "00"}), la couleur retournée est \code{bg_hex}.
\item Les valeurs sont arrondies mathématiquement pour éviter les artefacts visuels.
}
}
\examples{
# Fusion avec fond blanc (défaut)
\dontrun{
library(chartegraphique.sser)
hex_alpha_to_rgb("#80FF00CC")  # Résultat: "#99FF33"

# Fusion avec fond gris clair
hex_alpha_to_rgb("#80FF00CC", "#DDDDDD")  # Résultat: "#8EEB2E"

# Gestion des erreurs
try(hex_alpha_to_rgb("#1234567"))  # Erreur: format invalide
}
}
